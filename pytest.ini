# Configuración para pytest
# Archivo de configuración para ejecutar pruebas unitarias

[tool:pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v 
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes

# Marcadores personalizados
markers =
    slow: marca las pruebas como lentas
    integration: marca las pruebas de integración
    unit: marca las pruebas unitarias
    visualization: marca las pruebas de visualización

# Directorio de salida para reportes
junit_family = xunit2